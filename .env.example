COMPOSE_PROJECT_NAME=inventory-automation
POSTGRES_DB=inventory
POSTGRES_USER=inventory_bootstrap
# Used only to bootstrap PostgreSQL; backend connects as inventory_app.
POSTGRES_IMAGE=postgres:18-alpine
APP_ENVIRONMENT=development
APP_PORT=8080
